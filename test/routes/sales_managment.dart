import 'package:flutter/material.dart';
import 'package:croco/croco.dart';



class SalesManagment extends StatelessWidget {
  const SalesManagment({
    Key? key
    }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
      ),
    );
  }
}
